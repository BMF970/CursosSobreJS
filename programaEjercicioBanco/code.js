
for (var billete of caja) {
    if(dinero === 0){
        Math.floor(dinero / b50.valor)

        entregado.push(newBillete(b.valor, papeles));
        dinero = dinero - (billete.valor*papeles);
    }else{

    }

}